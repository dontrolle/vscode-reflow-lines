{
    "name": "reflow-paragraph",
    "displayName": "Reflow paragraph",
    "description": "Format lines in paragraph to a preffered line-length.",
    "version": "0.0.1",
    "publisher": "TroelsDamgaard",
    "engines": {
        "vscode": "^1.5.0"
    },
    "categories": [
        "Other"
    ],
	"keywords": [
		"markdown",
        "text",
		"format",
        "reflow",
		"pretty",
		"beautify"
	],    
	"repository": {
		"type": "git",
		"url": "https://github.com/TODO TODOTODO"
	},
    "activationEvents": [
        "onCommand:extension.reflowParagraph"
    ],
    "main": "./out/src/extension",
    "contributes": {
        "configuration": {
            "type": "object",
            "title": "Reflow paragraph configuration",
            "properties": {
                "reflow.preferredLineLength" :{
                    "type": "number",
                    "default": 80,
                    "description": "Preferred line length for reflowing paragraph.",
                    "minimum": 0
                }
            }
        },
        "commands": [{
            "command": "extension.reflowParagraph",
            "title": "Reflow paragraph"
        }]
    },
    "scripts": {
        "vscode:prepublish": "tsc -p ./",
        "compile": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install"
    },
    "devDependencies": {
        "typescript": "^2.0.3",
        "vscode": "^1.0.0",
        "mocha": "^2.3.3",
        "@types/node": "^6.0.40",
        "@types/mocha": "^2.2.32"
    }
}